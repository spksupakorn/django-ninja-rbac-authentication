# Understood RBAC policy centralization

The learner understands that adding a permission to a Role automatically affects every user linked
to that Role through `UserRole`; individual user records do not need updating. This establishes the
core policy-centralization benefit of the project's RBAC model.
