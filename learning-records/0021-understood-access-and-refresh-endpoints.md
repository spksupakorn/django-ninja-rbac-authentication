# Understood access and refresh endpoint roles

The learner understands that access tokens authenticate/authorize protected requests such as
`/auth/me`, while refresh tokens are only credentials for obtaining a new token pair. This ties
token purpose to the M7 endpoint contract.
