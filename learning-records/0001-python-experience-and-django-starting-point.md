# Python experience, Django starting point

The learner has written Python before but is not yet familiar with Django. Future lessons can use
normal Python concepts directly while explicitly introducing Django's request flow, settings, apps,
and ORM conventions.
