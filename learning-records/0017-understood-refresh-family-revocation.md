# Understood refresh-token family revocation

The learner understands that reuse of a revoked refresh token is a security signal affecting the
whole login-session family. Revoking the family invalidates still-active descendants and prevents a
possibly stolen credential chain from continuing to refresh tokens.
