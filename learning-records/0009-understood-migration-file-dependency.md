# Understood migration file dependency

The learner understands that a model change must first have a migration file before it can affect a
database. They know `migrate` applies existing migration files rather than generating schema changes
directly from `models.py`.
