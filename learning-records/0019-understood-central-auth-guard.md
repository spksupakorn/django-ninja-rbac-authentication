# Understood the central auth-guard benefit

The learner understands that deriving permissions once in `request.auth` avoids repeated JWT/DB
work and makes authorization policy consistent and visible across routes through a central guard.
