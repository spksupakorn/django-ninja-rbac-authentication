# Understood the entry-point distinction

The learner confirmed the distinction between `manage.py` for management commands and
`config.asgi:application` for Uvicorn-served HTTP requests. Future explanations can use this
shared-settings, separate-entry-points model without reintroducing the distinction.
