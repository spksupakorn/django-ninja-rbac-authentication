# Identified the request-flow entry points

The learner correctly identified `config.asgi.py`, `config.urls.py`, and `config.api.py` as the
core files in the `/api/health` request path. The next lesson can deepen this by showing the
settings-loading step that connects the ASGI entry point to URL routing.
