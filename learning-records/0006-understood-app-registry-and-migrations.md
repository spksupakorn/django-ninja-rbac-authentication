# Understood app registration and migrations

The learner correctly concluded that an unregistered `apps.accounts` would not produce migrations.
They now understand that Django discovers models for migration generation through the app registry
built from `INSTALLED_APPS`.
